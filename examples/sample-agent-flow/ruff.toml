line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "I", "W", "UP", "B"]

[lint.per-file-ignores]
# Tests adjust sys.path before importing the local modules under test.
"tests/*" = ["E402"]
